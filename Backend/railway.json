{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "bash download_models.sh && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
  }
}
